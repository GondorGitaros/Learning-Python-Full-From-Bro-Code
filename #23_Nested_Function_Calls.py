# nested functions calls = function calls inside other function calls. innermost function calls are resolved first. returned valur is used as argument for the next outer function


print(round(abs(float(input("Enter a whole positive number: ")))))
